import React from 'react';
import ButterflyList from '../List';

const styles = require('./list.css').default;

const Wishlist = () => (
  <ButterflyList readonlyMode={true}/>
);

export default Wishlist;
